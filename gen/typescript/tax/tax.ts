// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.12.0
//   protoc               v3.21.12
// source: tax/tax.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";

export const protobufPackage = "tax.v1";

export interface CalculateTaxRequest {
  annualIncome: number;
  taxYear: string;
  countryCode: string;
}

export interface CalculateTaxResponse {
  taxableIncome: number;
  taxOwed: number;
  effectiveTaxRate: number;
}

function createBaseCalculateTaxRequest(): CalculateTaxRequest {
  return { annualIncome: 0, taxYear: "", countryCode: "" };
}

export const CalculateTaxRequest: MessageFns<CalculateTaxRequest> = {
  encode(message: CalculateTaxRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.annualIncome !== 0) {
      writer.uint32(9).double(message.annualIncome);
    }
    if (message.taxYear !== "") {
      writer.uint32(18).string(message.taxYear);
    }
    if (message.countryCode !== "") {
      writer.uint32(26).string(message.countryCode);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): CalculateTaxRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseCalculateTaxRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 9) {
            break;
          }

          message.annualIncome = reader.double();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.taxYear = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.countryCode = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): CalculateTaxRequest {
    return {
      annualIncome: isSet(object.annualIncome)
        ? globalThis.Number(object.annualIncome)
        : isSet(object.annual_income)
        ? globalThis.Number(object.annual_income)
        : 0,
      taxYear: isSet(object.taxYear)
        ? globalThis.String(object.taxYear)
        : isSet(object.tax_year)
        ? globalThis.String(object.tax_year)
        : "",
      countryCode: isSet(object.countryCode)
        ? globalThis.String(object.countryCode)
        : isSet(object.country_code)
        ? globalThis.String(object.country_code)
        : "",
    };
  },

  toJSON(message: CalculateTaxRequest): unknown {
    const obj: any = {};
    if (message.annualIncome !== 0) {
      obj.annualIncome = message.annualIncome;
    }
    if (message.taxYear !== "") {
      obj.taxYear = message.taxYear;
    }
    if (message.countryCode !== "") {
      obj.countryCode = message.countryCode;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<CalculateTaxRequest>, I>>(base?: I): CalculateTaxRequest {
    return CalculateTaxRequest.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<CalculateTaxRequest>, I>>(object: I): CalculateTaxRequest {
    const message = createBaseCalculateTaxRequest();
    message.annualIncome = object.annualIncome ?? 0;
    message.taxYear = object.taxYear ?? "";
    message.countryCode = object.countryCode ?? "";
    return message;
  },
};

function createBaseCalculateTaxResponse(): CalculateTaxResponse {
  return { taxableIncome: 0, taxOwed: 0, effectiveTaxRate: 0 };
}

export const CalculateTaxResponse: MessageFns<CalculateTaxResponse> = {
  encode(message: CalculateTaxResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.taxableIncome !== 0) {
      writer.uint32(9).double(message.taxableIncome);
    }
    if (message.taxOwed !== 0) {
      writer.uint32(17).double(message.taxOwed);
    }
    if (message.effectiveTaxRate !== 0) {
      writer.uint32(25).double(message.effectiveTaxRate);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): CalculateTaxResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseCalculateTaxResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 9) {
            break;
          }

          message.taxableIncome = reader.double();
          continue;
        }
        case 2: {
          if (tag !== 17) {
            break;
          }

          message.taxOwed = reader.double();
          continue;
        }
        case 3: {
          if (tag !== 25) {
            break;
          }

          message.effectiveTaxRate = reader.double();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): CalculateTaxResponse {
    return {
      taxableIncome: isSet(object.taxableIncome)
        ? globalThis.Number(object.taxableIncome)
        : isSet(object.taxable_income)
        ? globalThis.Number(object.taxable_income)
        : 0,
      taxOwed: isSet(object.taxOwed)
        ? globalThis.Number(object.taxOwed)
        : isSet(object.tax_owed)
        ? globalThis.Number(object.tax_owed)
        : 0,
      effectiveTaxRate: isSet(object.effectiveTaxRate)
        ? globalThis.Number(object.effectiveTaxRate)
        : isSet(object.effective_tax_rate)
        ? globalThis.Number(object.effective_tax_rate)
        : 0,
    };
  },

  toJSON(message: CalculateTaxResponse): unknown {
    const obj: any = {};
    if (message.taxableIncome !== 0) {
      obj.taxableIncome = message.taxableIncome;
    }
    if (message.taxOwed !== 0) {
      obj.taxOwed = message.taxOwed;
    }
    if (message.effectiveTaxRate !== 0) {
      obj.effectiveTaxRate = message.effectiveTaxRate;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<CalculateTaxResponse>, I>>(base?: I): CalculateTaxResponse {
    return CalculateTaxResponse.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<CalculateTaxResponse>, I>>(object: I): CalculateTaxResponse {
    const message = createBaseCalculateTaxResponse();
    message.taxableIncome = object.taxableIncome ?? 0;
    message.taxOwed = object.taxOwed ?? 0;
    message.effectiveTaxRate = object.effectiveTaxRate ?? 0;
    return message;
  },
};

export type TaxCalculatorDefinition = typeof TaxCalculatorDefinition;
export const TaxCalculatorDefinition = {
  name: "TaxCalculator",
  fullName: "tax.v1.TaxCalculator",
  methods: {
    calculateTax: {
      name: "CalculateTax",
      requestType: CalculateTaxRequest as typeof CalculateTaxRequest,
      requestStream: false,
      responseType: CalculateTaxResponse as typeof CalculateTaxResponse,
      responseStream: false,
      options: {},
    },
  },
} as const;

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

export type DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

type KeysOfUnion<T> = T extends T ? keyof T : never;
export type Exact<P, I extends P> = P extends Builtin ? P
  : P & { [K in keyof P]: Exact<P[K], I[K]> } & { [K in Exclude<keyof I, KeysOfUnion<P>>]: never };

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}

export interface MessageFns<T> {
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  fromJSON(object: any): T;
  toJSON(message: T): unknown;
  create<I extends Exact<DeepPartial<T>, I>>(base?: I): T;
  fromPartial<I extends Exact<DeepPartial<T>, I>>(object: I): T;
}
